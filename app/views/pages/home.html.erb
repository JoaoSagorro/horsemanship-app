<% if !user_signed_in? %>
  <%= link_to "About", "/about" %>
  <%= link_to "School", "/school"%>
<% else %>
  <%# TODO: change email to user_name %>
  <%= "Welcome #{current_user.first_name} #{current_user.last_name} ->  #{current_user.role}" %>
  <%= link_to "Today", today_path %>
<% end %>
