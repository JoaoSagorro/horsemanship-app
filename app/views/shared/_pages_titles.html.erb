<% if controller_name === "aulas" && action_name === "index" %>
  <h1>Aulas</h1>
<% elsif controller_name === "aulas" && action_name === "today" %>
  <h1>Hoje</h1>
<% elsif controller_name === "aulas" && action_name === "show" %>
  <h1><%= @aula.title %></h1>
<% elsif controller_name === "aulas" && action_name === "new" %>
  <h1>Nova aula</h1>
<% end %>
